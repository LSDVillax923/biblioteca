# Si no esta creado el entorno virtual
python3 -m venv venv

# Iniciar el entorno virtual
source venv/bin/activate

# Si no se ha instalado zmq
pip install pyzmq

# Inicializar DB
python db_init.py

# Iniciar Gestor
python gestor.py "*:5555" "*:5560"

# Iniciar Actor (cada máquina sede con su copia DB)
python actor.py <ip gestor> 5560

# Iniciar PS 
python solicitante.py PS1 sample_requests.txt 172.20.10.2 5555

sqlite3 library.db "SELECT book_code, user_id FROM loans LIMIT 10;"